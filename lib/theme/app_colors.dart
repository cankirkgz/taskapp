import 'package:flutter/material.dart';

class AppColors {
  // Texts
  static const Color hintText = Color(0xFFADAEBC);
  static const Color textPrimary = Color(0xFF212121);
  static const Color text = Color(0xFF808080);
  static const Color unvalidText = Color(0xFF374151);
  static const Color titleColor = Color(0xFF374151);

  // Borders
  static const Color border = Color(0xFFE5E7EB);

  // Backgrounds
  static const Color background = Color(0xFFF9FAFB);

  static const Color primary = Color(0xFF3F51B5);

  static const Color white = Color(0xFFFFFFFF);

  // Icons
  static const Color icon = Color(0xFF808080);

  // Buttons
  static const Color confirmButton = Color(0xFF2563EB);
  static const Color cancelButton = Color(0xFFF3F4F6);
}
